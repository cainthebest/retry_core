# Examples

This directory collects runnable and documented retry use cases.

| Example | Idea |
|---------|------|
| `expanded_readme.rs` (if present) | Mirrors README quickstart |
| See crate docs | Builder / policy / backoff patterns |

Run with `cargo run --example <name>` when the example is registered in Cargo.toml.
